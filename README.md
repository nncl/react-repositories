# Repositories

## Getting Started

Install dependencies through:

```
$ yarn
```

Then execute in development mode:

```
$ yarn start
```

When ready to be deployed, run build:

```
$ yarn build
```
